- title t '_devise_sign_up'
.row
  .large-12.columns
    %h4= t '_devise_sign_up'
    %hr
.row
  .large-8.columns
    = devise_error_messages!
    .panel.secondary.hide-on-phones
      = form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
        = f.label :login
        %br
        = f.text_field :login, :autofocus => true
        = f.label :email
        %br
        = f.email_field :email
        = f.label :password
        %br
        = f.password_field :password, :label => 'Whatever name you want..'
        = f.label :password_confirmation
        %br
        = f.password_field :password_confirmation
        = f.submit t('helpers.submit.sign_up'), :class => 'button'
    %ul.breadcrumbs
      %li
        = link_to t('_devise_sign_in'), new_user_session_path, :method => :get
      %li
        = link_to t('_devise_password_new'), new_user_password_path, :method => :get
  .large-4.columns
    = image_tag ('warning.png')
    %h4 Обратите внимание!
    %p
      %b Логин
      может содержать только буквы латинского алфавита и цифры. Запрещено использовать пробел и прочие символы.
      %br/
      %b E-mail
      адрес будет использоваться системой для восстановления пароля.
