= form_for :guestbook, remote: true, :method => :post do |f|
  = f.text_area :message
  = f.submit t('helpers.submit.send'), :class => 'button', data: { disable_with: t('helpers.submit.processing') }
